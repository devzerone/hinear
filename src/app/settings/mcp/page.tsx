// TODO: Implement MCP token management UI
// This page will allow users to:
// - List their active MCP tokens
// - Issue new tokens
// - Revoke existing tokens
// - Download .env.local file with token

export default function MCPSettingsPage() {
  return (
    <div className="container mx-auto py-8">
      <h1 className="text-3xl font-bold mb-4">MCP Token Management</h1>
      <p className="text-muted-foreground">
        Token management UI coming soon. Use the API endpoints directly for now.
      </p>
    </div>
  );
}
